// extraction/page.js
"use client"; // Obligatorio para componentes que manejan eventos del cliente

import Extraction from "./Extraction"; // Importar el componente Extraction

export default function ExtractionPage() {
  return (
    <div>
      <Extraction /> {/* Usar el componente Extraction */}
    </div>
  );
}
